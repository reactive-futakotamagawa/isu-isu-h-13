---
sort: sum                       # max|min|avg|sum|count|uri|method|max-body|min-body|avg-body|sum-body|p1|p50|p99|stddev
query_string: true              # boolean
query_string_ignore_values: # boolean
decode_uri:                 # boolean
format: table                    # string
limit:                      # 5000
#noheaders:                  # boolean
#show_footers:               # boolean
#filters:                    # string
output: count,5xx,method,uri,sum,avg,min,max,p90,p99        # string(comma separated)
#pos_file:                   # string
#nosave_pos:                 # boolean
#percentiles:                # array

# ここを変える。正規表現を使う
# 例
# - /api/isu/.+^
# - /isu/[0-9a-zA-Z-]+/condition^
matching_groups:            # array
  - 